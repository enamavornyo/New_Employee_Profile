@model New_Employee_Profile.Models.NewEmployee

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>NewEmployee</h4>
    <hr />
    <div class="col-4">
            <img src="/media/newEmployees/@Url.Content(Model.ImageName)" class="mx-auto d-block img-fluid" alt="" />
            <h4>@Model.FName</h4>
            @*<div>
                @Html.Raw(item.Description)
            </div>
            <p>
                @item.Available
            </p>
            
        </div>
</div>
<div>
    @*<a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |*@
    <a asp-action="Index">Back to List</a>
</div>
